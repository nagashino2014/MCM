---
description: MCM UI는 Precision 기준을 따르며 cdash 컴포넌트와 의미 토큰을 재사용한다.
globs: frontend/**/*.{tsx,ts,jsx,js,css}
alwaysApply: false
---

# MCM UI — Precision

2026-09-10 사용자 요청으로 기존 Modernize/Soft Glass Ink의 시각 규칙을 대체한다. [전체 기준](../../docs/ui-precision-standard.md)을 먼저 읽는다.

- 무광 불투명 면, 1px 중립 경계, 카드/표 8px, 버튼·태그형 버튼·탭 버튼/입력 8px. 반경은 `--mcm-action-radius`를 사용하며 개별 버튼의 rounded 값으로 덮어쓰지 않는다. 비 button 동작 요소는 `cd-action`으로 표시한다.
- 카드 그림자·blur·배경 그라데이션·분류별 장식색 금지. 주요 버튼의 기존 블루·보라 색은 사용자 요청으로 유지. 팝업의 중립 그림자와 상태 배지는 허용.
- 공용 cdash 토큰을 사용하며 화면별 토큰 재정의 금지. 새로운 프레임워크/의존성 추가 금지.
- 기존 주요 동작의 색과 배치를 유지하고 신규 작업면에서는 핵심 동작 하나를 우선한다. 제목 22px, 본문 14px, 입력 13px/36px, 표 행 최소 44px.
- 메뉴의 기존 폭·레일·그룹·자식·현재 위치를 유지한다.
- 긴 설명은 CdHelp, 짧은 현재 맥락은 subtitle. 오류·필수 입력·실행 영향은 숨기지 않는다.
- 기능·URL·역할·날짜 입력·미저장 변경·API 동작을 보존한다.
- 실제 컴포넌트로 라이트/다크·390px·키보드·200% 확대를 검증하고 미검증을 명시한다.

**최우선: 사용자의 명시적 지시 없이 기존 UI 구조를 변경하지 않는다.** 트리뷰·상세 양식·카드 배치·표 열·탭·분할 비율·스크롤 부모를 보존하고 컴포넌트의 글자·색·경계·간격만 정리한다. 허용된 구조 변경은 대시보드와 영업 상세의 FHD 하단 재배치 두 건이며 세부사항은 UI 기준 §7을 따른다.
