# 개인정보 보호·통신 데이터 컴플라이언스 블루프린트 (PR)

> 2026-08-19 작성. 배경: 전 임직원 메일 송수신 기록 조회 기능을 검토했으나 법률 리스크(통신비밀보호법·개인정보보호법·판례)가
> 커서 철회하고, 대신 **현재 그룹웨어의 개인정보 관리 체계와 메일·메신저 데이터의 전송·보관·열람 권한을 판례 요건에 맞게
> 정비**하기로 결정(사용자 확정). 법리 근거는 Claude 법률 상담 대화(2026-08-19 공유본) 요약을 §1에 수록.

---

## 1. 법리 요약 — 시스템 요구사항으로 번역된 판례

### 1.1 적용 법령 지도

| 법령 | 규율 대상 | 리스크 크기 |
|---|---|---|
| 통신비밀보호법 §3·§16 | **실시간** 감청(송수신과 동시) | 1~10년 징역(벌금형 없음) — 질적으로 최상위 |
| 정보통신망법 §48①(침입)·§49(비밀 침해) | 계정 무단 접속·비밀 취득 | 5년 이하 징역/5천만원 이하 벌금 |
| 형법 §316② | 기술적 수단으로 전자기록 취득 | 3년 이하 징역 |
| 개인정보보호법 §15·§18·§29·§30 | 수집·이용 근거, 목적 외 이용, 안전조치, 접속기록 | 형사처벌 + 매출액 기준 과징금 |
| 근로자참여법 §20①14호 | 근로자 감시 설비 설치 = 노사협의 사항(**주된 목적 불문**) | 절차 하자 |
| 근로기준법 §93·§94 | 취업규칙 불이익 변경 → 과반수 동의 | 절차 하자 |

### 1.2 핵심 판례 → 설계 함의

| 판례 | 요지 | MCM 설계 함의 |
|---|---|---|
| 대법원 2012도4644 등 | '감청'은 송수신과 **동시**에 이루어지는 것만. 보관 중 데이터 사후 열람은 감청 아님 | **실시간 훅(inline tap) 절대 금지** — 저장 후 비동기 처리만. 현재 아키텍처(SES→S3→틱 폴링) 적합 |
| 대법원 2009. 12. 14. 2007도6243 | 구체적 혐의 + 다른 수단 소진 + 최소범위 열람 = 정당행위 | 사후 표적 조사만 허용. **상시 모니터링 근거로 원용 불가** |
| 대구지법 2024. 8. 21. 2023나320254 | 검색기록·앱 로그 등 **메타데이터 무단 탐지도 불법행위** | "본문 안 보고 로그만 본다"는 안전지대 아님 → 메타 조회에도 근거 규정 + 사유 기록 |
| 사내 감사 적법 인정례 | ①서약서 동의 ②메타 1차 스크리닝 ③키워드·기간 한정 ④2인 통제 ⑤최소 본문 열람 ⑥누설금지 서약 ⑦표적 징계 없음 | break-glass 승인 워크플로의 표준형(§4 P2) |
| 대법원 퇴직자 이메일 파기환송 | §49 '타인의 비밀' 축소해석. 단 **§48① 침입죄는 별개로 성립** | 퇴직자 계정: 잠금(차단)과 열람(취득)을 분리, 열람은 break-glass 경유 |

### 1.3 리스크 등급 (MCM 기능 매핑)

| 행위 | 등급 | MCM 현황 |
|---|---|---|
| 메신저 실시간 감시 | 🔴 통비법 — 회피 불가 | 해당 없음(구현 금지 원칙 유지) |
| 메일 본문 상시 스캔 후 열람 | 🔴 목적 초과 | 해당 없음 — **전 임직원 메일 조회 기능 철회 확정** |
| 규정·동의 없는 로그 상시 수집 | 🟠 불법행위 인정례 | 파일함(첨부 메타+실물) — §3.1 G1, 문서 정비 필요 |
| 규정+동의 기반 메타 로깅, 열람 승인제 | 🟡 설계 품질에 좌우 | 목표 상태(§4) |
| 혐의 발생 후 한정 조사(2인 통제) | 🟢 적법 인정 가능 | break-glass 로 절차화(§4 P2) |

---

## 2. 현황 진단 (2026-08-19 코드 실사)

### 2.1 잘 되어 있는 것 (유지)

| 영역 | 근거 |
|---|---|
| 메일 본인 스코프 | 전 메일 API가 `requireSession()` → 본인 mailbox 로 한정. **타인 메일 본문 열람 API 자체가 없음** (`lib/mail/messages.ts` 전 쿼리 `AND mailbox_id = $2`) |
| 메신저 방 멤버십 | `lib/chat/queries.ts` 전 함수 `memberRow()` 강제. `chat.*` 관리자 권한키 자체가 없어 role 로 뚫을 경로 없음 |
| 실시간 훅 부재 | 메일 수신 = SES→S3→SQS→1분 틱 폴링(저장 후 처리). 감청 구성요건 원천 회피 |
| RBAC | 권한 템플릿 기반(`requirePermission`), admin role 자동 통과 폐지(마이그 111) |
| 변경 감사 | `audit_log` + `recordAuditLog` 117종(결재·계약·인사 변경 계열) |

### 2.2 갭 (우선순위순)

| # | 갭 | 상세 | 대응 |
|---|---|---|---|
| G1 | **파일함(/files)의 통신 첨부 접근 통제** | `files.manage` 하나로 전 대화방 첨부 목록·실물 열람·하드 삭제 가능. 멤버십 필터 없음(스토리지 관리 기능으로서 의도된 설계 — 다우오피스 등 업계 표준). ~~감사로그 없음~~ → **08-19 열람·삭제 감사로그 반영 완료** | P0(완료)+P5 문서 근거 |
| G2 | **접속기록 체계 부재** | 열람 이력 0종·로그인 성공 이력 미기록(실패만, 1일 자동 삭제)·`audit_log` append-only 보호 없음. 개보법 시행령 접속기록(1년+) 요건 미충족 | P1 |
| G3 | **주민번호 취급** | `employee_profiles.resident_registration_encrypted`(AES-GCM, 단 키가 세션 시크릿/하드코딩 폴백), `payroll.rrn_prefix` **평문** | P3 |
| G4 | **보유기간·파기 부재** | 메일 정본(DB 평문)·메신저 메시지 무기한. 메시지 삭제 API 미구현(soft-delete 컬럼만). 원문 S3만 90일 lifecycle | P4 |
| G5 | **문서 세트 부재** | 취업규칙 모니터링 조항·개인정보 처리방침·정보보호 서약서 없음(설계 의도만 docs 에 존재) | P5 |
| G6 | **이력 없는 직접 경로** | bastion SSM→RDS 직접 쿼리, 단일 S3 버킷 콘솔 접근 — 모두 무기록. RDS Activity Streams·CloudTrail data events·S3 access logging 미설정 | P6 |
| G7 | **비상 우회 스위치** | `RBAC_ENFORCE=false`·`RBAC_ADMIN_BYPASS=true` 환경변수로 전 권한 통과 가능 | P1에 포함(사용 시 감사로그) |
| G8 | **리포 내 비밀** | 루트에 `ADMIN_PASSWOR.txt` 커밋됨 | **P0 즉시** |
| G9 | 별칭 가로채기 잠재 경로 | `mail.manage` 로 타인 수신 별칭을 자기 mailbox 에 연결 가능(향후 수신분) | P1(별칭 변경 감사로그) |

---

## 3. 설계 원칙 (전 모듈 공통 — 신규 기능 개발 시 준수)

1. **실시간 훅 금지**: 전송 파이프라인(SMTP·웹소켓·메시지 발송 경로)에 inline 감시 로직을 넣지 않는다. 스캔이 필요하면 저장 완료 후 비동기 배치.
2. **본인 스코프 기본**: 통신 데이터(메일 본문·메신저 메시지)는 본인/방 멤버 스코프를 유지한다. 관리자용 전사 조회 API 를 만들지 않는다.
3. **메타와 본문의 2단 분리**: 관리 기능(파일함 등)은 메타데이터+파일 단위까지만. 메일 본문·대화 내용 접근 기능은 break-glass(§4 P2) 없이 만들지 않는다.
4. **열람도 기록**: 타인 데이터에 닿는 모든 조회·열람·삭제는 `audit_log` 기록을 동반한다(파일함은 적용 완료 — `file_library_open`/`file_library_delete`).
5. **자동 결정에 사람 개입**: 자동 탐지(향후 DLP 류)가 계정 정지·인사 조치로 직결되는 설계 금지(개보법 §37조의2).
6. **사후 통지**: 표적 조사 종료 후 대상자 통지 절차를 둔다(§4 P2 워크플로에 내장).
7. **퇴직자**: 계정 잠금(즉시)과 데이터 열람(break-glass)을 분리한다.
8. **문서 우선**: 모니터링 성격의 신규 기능은 취업규칙·처리방침·서약서 문안 반영 후 배포한다.

---

## 4. 로드맵

### PR-P0 — 즉시 조치 (½일) ✅ 일부 완료
- [x] 파일함 열람·삭제 감사로그(`file_library_open`·`file_library_delete`·`file_retention_purge`) — 08-19 반영
- [ ] `ADMIN_PASSWOR.txt` 리포 제거 + 해당 비밀 로테이션(git 이력에 남으므로 값 변경이 본질) + `.gitignore` 등재
- [ ] `RBAC_ENFORCE`·`RBAC_ADMIN_BYPASS` 현재 staging 환경변수 값 점검(비상 모드 꺼짐 확인)

### PR-P1 — 접속기록(개보법 시행령 요건) (2~3일)
- `access_log` 신설(마이그): `actor_user_id, action(view|download|delete|login|export), target_kind, target_id, detail jsonb, created_at` — **INSERT 전용**
  - append-only: 앱 DB 계정에 `REVOKE UPDATE, DELETE ON access_log` + 월 파티션(1년 이상 보관, 삭제는 파티션 DROP 을 별도 운영 계정으로만)
- 기록 지점: 로그인 성공(현재 실패만 기록) / 파일함·개인문서함 외 **타인 개인정보 화면**(인사서류 다운로드, 급여 조회, 연말정산 열람, 근태 상세) / 메일 별칭 변경(G9)
- `RBAC_ENFORCE=false`·`ADMIN_BYPASS` 기동 시 경고 로그 + access_log 1행
- 열람 UI: 관리 메뉴에 "접속기록 조회"(admin, 조회 자체도 기록)

### PR-P2 — break-glass 열람 승인 워크플로 (3~4일, 필요 시점에)
> 현재 타인 메일·메신저 본문 열람 경로가 코드에 없으므로 **당장 만들 필요는 없음**. 유출 사고 조사 요구가 실제로 생기면
> 이 설계로만 구현한다(그 전까지는 "기능 없음"이 최선의 방어).
- `investigation_requests`: 신청자·승인자(2인 이상)·대상자·사유·기간 범위·사전 등록 키워드·열람 건수 상한
- 1차 메타 스크리닝(제목·파일명·시각) → 매칭 건만 본문 열람 허용, 전 과정 access_log
- 종료 시 대상자 통지 레코드 + 결과 보고서(열람 건수·범위) 자동 생성
- 퇴직자 계정 열람도 동일 워크플로 경유

### PR-P3 — 암호화·키 관리 (1~2일)
- `EMPLOYEE_PII_ENCRYPTION_KEY` 전용 키를 Secrets Manager 로 발급·주입(세션 시크릿/하드코딩 폴백 제거 — 폴백 시 기동 실패로 변경)
- `payroll.rrn_prefix`·`dependents_detail` 내 rrnPrefix 암호화 마이그레이션(기존 평문 재암호화 스크립트)
- S3 두 버킷에 `aws_s3_bucket_server_side_encryption_configuration` 명시(terraform, `-target` apply — SES drift 주의)
- RDS `storage_encrypted` 확인(미적용이면 스냅샷 복원 절차 별도 계획)

### PR-P4 — 보유기간·파기 (1~2일)
- 파일 저장 정책(190·191, 완료된 크기 기반)에 더해 **통신 데이터 보존 정책**: 메일 정본·메신저 메시지 보존연한 설정(기본 영구, 관리자 설정 시 파기 배치) — file-retention-tick 패턴 재사용
- 메신저 메시지 삭제 API(소프트삭제 기록) + 소프트삭제 N일 후 본문 NULL 화 배치
- 퇴사자 개인문서함·개인 영수증 파기 절차(퇴직 처리 플로우에 연결)

### PR-P5 — 3중 문서 세트 (문안 작업 + 노무사 검토)
- **취업규칙**: 정보시스템 이용·모니터링 근거 조항(파일함 스토리지 관리 목적의 첨부 목록 조회·정리 포함, 조사 절차·범위·통지) — 상시 10인 이상이므로 **과반수 동의** 필요
- **개인정보 처리방침**(사내 공지): 수집 항목 인벤토리(§2 실사 결과 활용)·보유기간·위탁(AWS)·정보주체 권리
- **정보보호 서약서**: 적법 인정례 문구 준용 — "영업비밀 보호·유출 방지·보안사고 대응을 위해 필요한 경우 사내 메일, 정보통신망 통신기록 등에 대한 점검·검색·감사 실시에 동의"
- 노사협의회(30인 이상 여부 확인) 협의 안건 상정 여부 판단
- ⚠ 최종 문안은 **노무사/변호사 검토 필수**(포괄 동의 무효 리스크 — 문구 하나로 갈림)

### PR-P6 — 인프라 접근 이력 (1일)
- S3 `app_data` 버킷 CloudTrail data events(또는 server access logging) 활성화
- RDS: bastion 경유 직접 쿼리 관행 축소 — 운영 조회는 앱 관리 화면으로 이관, 직접 접속 시 기록이 남는 Database Activity Streams 는 비용 검토 후
- 메일 첨부·인사서류·급여·채팅 첨부의 S3 프리픽스별 IAM 최소화(장기 과제)

---

## 5. 파일함·개인문서함의 위치 (08-19 구현분과의 정합)

- **파일함(/files)**: 스토리지 관리 목적의 전사 첨부 목록·열람·삭제. 업계 표준 기능(다우오피스·아마란스10·비즈메카 동등 기능 보유)으로 유지하되, ① `files.manage`(is_dangerous) 권한 한정 ② 열람·삭제 감사로그(적용 완료) ③ P5 문서에 목적·범위 명시로 정당화한다. 메일 첨부는 소스에 넣지 않았고(메일함 용량 관리로 갈음 — 사용자 확정), 메신저 **본문**은 어떤 경로로도 노출하지 않는다.
- **개인문서함(/files/personal)**: 본인 스코프 전용(관리자 열람 경로 없음). 파일함 통합 목록·자동 삭제 배치 대상에서 **제외** — 개인 공간 최소침해 원칙.
- **자동 삭제 배치(file-retention-tick)**: 정책 기본값 '영구'라 명시적으로 정책을 바꾼 경우에만 동작. 실행 이력은 `file_retention_runs` + audit_log 에 남는다.

## 6. 다음 액션 (사용자 결정 대기)

1. P0 잔여 2건(ADMIN_PASSWOR.txt 제거·로테이션, 비상 스위치 점검) — 즉시 실행 권장
2. P1 접속기록부터 착수할지, P5 문서 세트(노무사 컨택)를 병행할지
3. P2 break-glass 는 실제 조사 수요 발생 전까지 보류(권장)
