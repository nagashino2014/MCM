# Claude 재검토 요청: RS-1 재실행 방어

기준 main `f98b08b5dea2095802da93464c8992f8c2a4bf88`의 격리 후보 `tmp/finance-feature-install-fix-20260922`를 검토해 달라. 이번 수정은 앞 회신 `implementation/docs/finance-staging-install-fix-claude-2026-09-22/claude-review-response.md`의 RS-1에 대한 보완이다. 스테이징·AWS 쓰기와 Next 배포는 하지 않았다. 배포 금지는 유지한다.

같은 폴더의 `CLAUDE-REVIEW-REQUEST.md`는 이전 14경로 후보를 검토할 때의 역사적 요청서다. **이번 후보는 `review-manifest.json`의 22경로**와 이 문서를 기준으로 읽어 달라.

## 바뀐 범위

- SQL 232~237 각각의 `BEGIN` 직후에 선행 검사를 두었다. 현재 스키마에 240이 설치하는 `finance_assert_r1_definitions(text)`가 있으면 55000으로 중단한다. 함수 재정의나 업무 데이터 변경보다 먼저 실행된다. 240 전의 초기 설치·부분 재개는 허용한다.
- 유지보수 스크립트의 설치 후 검사를 기존 3개에서 최종 정의 검사 7개 전체로 넓혔다. 존재하는 검사만 호출하므로 부분 설치 시점에도 작동한다.
- 재개 문서에 242·243 미설치 확인, 파일 단위 실패 후 재개, 232~237 재실행 금지를 명시했다. 기존 21개 적용 순서는 유지했다.

## Codex 격리 시험

- 실제 스테이징의 226~232 적용 후 **스키마만** 복제한 기존 PostgreSQL 17 시험 환경을 썼다. 실제 업무 데이터는 복사하지 않았다.
- 완성된 별도 격리 DB에서 232~237을 **한 파일씩** 재실행했다. 6개 모두 exit 비정상·55000이고 각 시도 뒤 정의 검사 7/7이 유지됐다. 235도 방어를 통일하기 위해 거절한다.
- 새 격리 DB에 226~232를 재적용한 뒤 재개 순서 21개를 적용했다. 7/7 최종 검사가 통과했다. 기존 검토의 233→243 역순 실패 및 순서 가드 판정은 이 수정으로 바뀌지 않는다.
- 이 시험은 실제 스테이징의 데이터 전체 설치나 AWS 유지보수 절차를 대체하지 않는다.

## 검토 질문

1. 232~237의 검사가 함수 재정의 **전에** 실행되고, 완성 후 재실행에서 55000으로 거절하며 최종 7/7을 보존하는가? 가능하면 앞 회신의 재실행 지도를 다시 확인해 달라.
2. 226~232 적용 후 스키마에서 21개 재개, 226~232 재적용 후 재개, 중간 실패 뒤 실패 파일부터 재개가 그대로 되는가? 240 이후 232~237 재실행이 필요한 정상 복구 경로가 있는가?
3. 스크립트의 7개 설치 후 검사는 부분 설치와 최종 설치 모두에서 안전한가? 각 파일이 이미 커밋된 뒤의 검사라는 한계를 문서가 정확히 표시하는가?
4. 원래 회신의 RS-1을 스테이징 재개 차단 사유에서 내릴 수 있는가? 남은 관찰(O-1, O-3)과 실제 데이터 읽기 확인은 별도로 남겨 달라.

러너를 실행한다면 인자·삭제 호출을 파일 전체에서 먼저 확인하고, 출력은 새 폴더로만 보내 달라. 제품 후보와 이전 봉인 패키지는 수정하지 말아 달라.
